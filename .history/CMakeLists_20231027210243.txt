set(CMAKE_C_COMPILER "path/to/c_compiler")
set(CMAKE_CXX_COMPILER "D:/Mingw-w64/mingw64/bin/g++.exe")
# CMake 最低版本号要求
cmake_minimum_required (VERSION 3.24.2)

# 项目信息
project (Demo2)

# 查找当前目录下的所有源文件
# 并将名称保存到 DIR_SRCS 变量
aux_source_directory(. DIR_SRCS)

# 指定生成目标
add_executable(Demo ${DIR_SRCS})